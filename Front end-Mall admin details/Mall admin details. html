<div class="container">
  <h2 class="text-center mb-4">Mall Admin Management</h2>
  <div class="row justify-content-center">
    <div class="col-lg-6 col-md-8 col-sm-12">
      <form #registerForm="ngForm" (ngSubmit)="register(registerForm)">
        <div class="card shadow-sm border-0 p-4 mb-4" style="background: #f9fafc; border-radius: 18px;">
          <h4 class="text-center mb-4" style="font-size: 2rem; font-weight: 800; color: #ff8c00; letter-spacing: 1px;">Add Mall Admin</h4>
          <div class="row g-3">
            <div class="col-12">
              <label for="username-form" class="form-label">Username</label>
              <input id="username-form" required #username="ngModel" [(ngModel)]="mallAdminForm.username" name="username" type="text" class="form-control" placeholder="Enter username" autocomplete="off">
              <div *ngIf="username.invalid && username.touched">
                <small class="text-danger" *ngIf="username.errors?.['required']">Required</small>
              </div>
            </div>
            <div class="col-12">
              <label for="password-form" class="form-label">Password</label>
              <input id="password-form" required #password="ngModel" [(ngModel)]="mallAdminForm.password" name="password" type="password" class="form-control" placeholder="Enter password" autocomplete="off">
              <div *ngIf="password.invalid && password.touched">
                <small class="text-danger" *ngIf="password.errors?.['required']">Required</small>
              </div>
            </div>
            <div class="col-12">
              <label for="email-form" class="form-label">Email</label>
              <input id="email-form" required #email="ngModel" [(ngModel)]="mallAdminForm.email" name="email" type="email" class="form-control" placeholder="Enter email" autocomplete="off">
              <div *ngIf="email.invalid && email.touched">
                <small class="text-danger" *ngIf="email.errors?.['required']">Required</small>
              </div>
            </div>
            <div class="col-12">
              <label for="loginAttempts-form" class="form-label">Login Attempts</label>
              <input id="loginAttempts-form" required #loginAttempts="ngModel" [(ngModel)]="mallAdminForm.loginAttempts" name="loginAttempts" type="number" class="form-control" placeholder="Enter login attempts" autocomplete="off">
              <div *ngIf="loginAttempts.invalid && loginAttempts.touched">
                <small class="text-danger" *ngIf="loginAttempts.errors?.['required']">Required</small>
              </div>
            </div>
            <div class="col-12 d-flex align-items-center mb-2">
              <input id="isActive-form" #isActive="ngModel" [(ngModel)]="mallAdminForm.isActive" name="isActive" type="checkbox" class="form-check-input me-2">
              <label for="isActive-form" class="form-check-label">Active</label>
            </div>
            <div class="col-12">
              <input class="btn btn-primary w-100" type="submit" value="Submit">
            </div>
          </div>
        </div>
      </form>
    </div>
  </div>

  <div class="row mt-5 justify-content-center">
    <div class="col-12">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th class="text-uppercase fw-bold">ID</th>
            <th class="text-uppercase fw-bold">Username</th>
            <th class="text-uppercase fw-bold">Email</th>
           <th class="text-uppercase fw-bold">Password</th>
            <th class="text-uppercase fw-bold">Login Attempts</th>
            <th class="text-uppercase fw-bold">Active</th>
            <th class="text-uppercase fw-bold">Last Login</th>
            <th class="text-uppercase fw-bold">Edit</th>
            <th class="text-uppercase fw-bold">Delete</th>
          </tr>
        </thead>
        <tbody>
          <tr *ngIf="validMallAdmins.length === 0">
            <td colspan="8" class="text-center">No valid admin records found. Please check your data.</td>
          </tr>
          <tr *ngFor="let admin of validMallAdmins">
            <td>{{admin.mallAdminId}}</td>
            <td>
              <span title="{{admin.username}}" style="max-width: 150px; display: inline-block; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-weight: bold;">{{admin.username}}</span>
            </td>
            <td>
              <span title="{{admin.email}}" style="max-width: 200px; display: inline-block; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-weight: bold;">{{admin.email}}</span>
            </td>
           <td>
             <span title="{{admin.password}}" style="max-width: 150px; display: inline-block; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-family: 'Fira Mono', monospace; letter-spacing: 1px;">{{admin.password}}</span>
           </td>
            <td>{{admin.loginAttempts}}</td>
            <td>{{admin.isActive ? 'Yes' : 'No'}}</td>
            <td>{{admin.lastLoginTime}}</td>
            <td>
              <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#exampleModal" (click)="editMallAdmin(admin)">Edit</button>
            </td>
            <td>
              <button type="button" class="btn btn-danger" (click)="deleteMallAdmin(admin)">Delete</button>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>

  <!-- Modal -->
  <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h1 class="modal-title fs-5" id="exampleModalLabel">Update Mall Admin</h1>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <form #updateForm="ngForm" (ngSubmit)="updateMallAdmin()">
            <h4 class="display-6 text-center py-2">Update Mall Admin</h4>
            <div class="mb-3">
              <label for="username-edit">Username</label>
              <input id="username-edit" required #username="ngModel" [(ngModel)]="mallAdminForm.username" name="username" type="text" class="form-control" placeholder="Enter username">
              <div *ngIf="username.invalid && username.touched">
                <small class="text-danger" *ngIf="username.errors?.['required']">Required</small>
              </div>
            </div>
            <div class="mb-3">
              <label for="password-edit">Password</label>
              <input id="password-edit" required #password="ngModel" [(ngModel)]="mallAdminForm.password" name="password" type="password" class="form-control" placeholder="Enter password">
              <div *ngIf="password.invalid && password.touched">
                <small class="text-danger" *ngIf="password.errors?.['required']">Required</small>
              </div>
            </div>
            <div class="mb-3">
              <label for="email-edit">Email</label>
              <input id="email-edit" required #email="ngModel" [(ngModel)]="mallAdminForm.email" name="email" type="email" class="form-control" placeholder="Enter email">
              <div *ngIf="email.invalid && email.touched">
                <small class="text-danger" *ngIf="email.errors?.['required']">Required</small>
              </div>
            </div>
            <div class="mb-3">
              <label for="loginAttempts-edit">Login Attempts</label>
              <input id="loginAttempts-edit" required #loginAttempts="ngModel" [(ngModel)]="mallAdminForm.loginAttempts" name="loginAttempts" type="number" class="form-control" placeholder="Enter login attempts">
              <div *ngIf="loginAttempts.invalid && loginAttempts.touched">
                <small class="text-danger" *ngIf="loginAttempts.errors?.['required']">Required</small>
              </div>
            </div>
            <div class="form-check mb-3">
              <input id="isActive-edit" #isActive="ngModel" [(ngModel)]="mallAdminForm.isActive" name="isActive" type="checkbox" class="form-check-input">
              <label for="isActive-edit" class="form-check-label">Active</label>
            </div>
            <div class="form-group text-center">
              <input class="btn btn-primary w-100" type="submit" value="Update">
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

</div>
